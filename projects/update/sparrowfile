#!raku

%*ENV<SP6_DUMP_TASK_CODE> = False;

#bash "git checkout --", %(
#   cwd => "{%*ENV<HOME>}/.sparky",
#);

git-scm tags()<SCM_URL>, %(
  to => "{%*ENV<HOME>}/.sparky",
  branch => tags<SCM_BRANCH>
);

bash "sudo service sparky start";
