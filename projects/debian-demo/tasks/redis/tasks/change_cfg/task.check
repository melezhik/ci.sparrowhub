source: /tmp/redis.conf.new

~regexp: ^^ (\x[23]) \s requirepass \s

code: <<RAKU
!raku

if matched() {
    my $ln = captures-full()[0]<index>;
    replace("/tmp/redis.conf.new",$ln,"requirepass foobar");
}

RAKU

code: <<BASH
!bash
sudo cp /tmp/redis.conf.new /etc/redis/redis.conf
BASH
