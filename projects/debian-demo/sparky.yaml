#crontab: "2 * * * *"
sparrowdo:
  no_sudo: true
  no_index_update: false
  bootstrap: false
  format: default
disabled: false
allow_manual_run: true
keep_builds: 5
vars:
  -
      name: scenario
      values: [ python-ssl, redis, nsd ]
      type: select
      default: python-ssl
